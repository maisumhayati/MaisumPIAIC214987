/* Number Eight: Write addition, subtraction, multiplication,
 and division operations that 
 each result in the number 8. 
 Be sure to enclose your operations in print statements to see the results. */

import { sum, subtract, divide, multiply } from "mathjs";

console.log(sum(5, 3)); //addition
console.log(subtract(11, 3)); //subtraction
console.log(divide(16, 2)); //division
console.log(multiply(4, 2)); //multiplication

export {};
